See [CLAUDE.md](./CLAUDE.md) for repo rules. They apply to every AI assistant
working on this repo — Codex, Gemini, OpenCode, Cursor, Cline, Continue,
anything else.

**Strongest rule: this is a public fork. Never write personal information
(names, emails, absolute paths containing a username, API keys, tokens,
hostnames of private infra, real database names, etc.) into any tracked
file.** Use placeholders like `<project-root>`, `user@example.com`,
`<your-api-key>` instead. Full rule list and substitution table in
`CLAUDE.md`.
